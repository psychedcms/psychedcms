version: '3'

vars:
  COMPOSE_FILE: '{{.ROOT_DIR}}/docker-compose.yml'
  PACKAGES_DIR: '{{.ROOT_DIR}}/packages'

tasks:
  # ===========================================================================
  # Composer / Dependencies
  # ===========================================================================
  install:
    desc: Install PHP dependencies
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} exec php composer install --prefer-source

  update:
    desc: Update PHP dependencies
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} exec php composer update --prefer-source

  require:
    desc: Add a new PHP dependency
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} exec php composer require {{.CLI_ARGS}}

  # ===========================================================================
  # Database
  # ===========================================================================
  migrate:
    desc: Run database migrations
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} exec php bin/console doctrine:migrations:migrate --no-interaction

  migrate:diff:
    desc: Generate a migration diff
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} exec php bin/console doctrine:migrations:diff

  migrate:status:
    desc: Show migration status
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} exec php bin/console doctrine:migrations:status

  db:create:
    desc: Create the database
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} exec php bin/console doctrine:database:create --if-not-exists

  db:drop:
    desc: Drop the database
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} exec php bin/console doctrine:database:drop --force --if-exists

  db:reset:
    desc: Reset the database (drop, create, migrate)
    cmds:
      - task: db:drop
      - task: db:create
      - task: migrate

  # ===========================================================================
  # Symfony Console
  # ===========================================================================
  console:
    desc: Run Symfony console command
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} exec php bin/console {{.CLI_ARGS}}

  cache:clear:
    desc: Clear Symfony cache
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} exec php bin/console cache:clear

  # ===========================================================================
  # Testing
  # ===========================================================================
  test:
    desc: Run PHPUnit tests
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} exec php bin/phpunit {{.CLI_ARGS}}

  test:coverage:
    desc: Run tests with coverage report
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} exec php bin/phpunit --coverage-html var/coverage

  # ===========================================================================
  # Code Quality
  # ===========================================================================
  lint:
    desc: Run all linting tools
    cmds:
      - task: phpstan
      - task: cs:check

  phpstan:
    desc: Run PHPStan static analysis
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} exec php vendor/bin/phpstan analyse

  cs:check:
    desc: Check code style with PHP-CS-Fixer
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} exec php vendor/bin/php-cs-fixer fix --dry-run --diff

  cs:fix:
    desc: Fix code style with PHP-CS-Fixer
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} exec php vendor/bin/php-cs-fixer fix

  # ===========================================================================
  # API Platform
  # ===========================================================================
  api:routes:
    desc: List all API routes
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} exec php bin/console debug:router --show-controllers | grep api

  # ===========================================================================
  # Package Testing - psychedcms-core
  # ===========================================================================
  test:core:
    desc: Run tests for psychedcms-core package
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} exec php bin/phpunit {{.PACKAGES_DIR}}/psychedcms-core/tests/

  # ===========================================================================
  # Git - psychedcms-core (atomic operations)
  # ===========================================================================
  git:status:core:
    desc: Show git status for psychedcms-core
    cmds:
      - echo "=== psychedcms-core ===" && git -C {{.PACKAGES_DIR}}/psychedcms-core status -s && git -C {{.PACKAGES_DIR}}/psychedcms-core log -1 --oneline

  git:branch:core:
    desc: Show current branch for psychedcms-core
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-core branch --show-current


  # Feature branch operations
  feature:ensure:core:
    desc: "Ensure feature branch exists on psychedcms-core, create if needed"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - ../scripts/git-ensure-branch.sh {{.PACKAGES_DIR}}/psychedcms-core feature/{{.FEATURE}}

  feature:checkout:core:
    desc: "Checkout feature branch on psychedcms-core"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-core checkout feature/{{.FEATURE}}

  # Commit operations (with lazy branch creation)
  commit:core:
    desc: "Commit psychedcms-core with feature reference (creates branch if needed)"
    vars:
      FEATURE: '{{.FEATURE}}'
      MESSAGE: '{{.MESSAGE}}'
    cmds:
      - task: feature:ensure:core
        vars:
          FEATURE: '{{.FEATURE}}'
      - cmd: 'git -C {{.PACKAGES_DIR}}/psychedcms-core add .'
      - cmd: 'git -C {{.PACKAGES_DIR}}/psychedcms-core commit -m "[feature/{{.FEATURE}}] core: {{.MESSAGE}}"'

  # Push/Pull operations
  push:core:
    desc: Push psychedcms-core current branch
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-core push -u origin $(git -C {{.PACKAGES_DIR}}/psychedcms-core branch --show-current)

  pull:core:
    desc: 'Pull psychedcms-core (current branch or specified via BRANCH=x)'
    vars:
      BRANCH:
        sh: echo "${BRANCH:-$(git -C {{.PACKAGES_DIR}}/psychedcms-core branch --show-current)}"
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-core checkout {{.BRANCH}}
      - git -C {{.PACKAGES_DIR}}/psychedcms-core pull origin {{.BRANCH}}

  rebase:core:
    desc: "Rebase current feature branch onto main on psychedcms-core (updates main, rebases, force pushes)"
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-core fetch -p origin main:main
      - git -C {{.PACKAGES_DIR}}/psychedcms-core rebase main
      - git -C {{.PACKAGES_DIR}}/psychedcms-core push --force-with-lease origin $(git -C {{.PACKAGES_DIR}}/psychedcms-core branch --show-current)

  # Merge operations
  feature:merge:core:
    desc: "Squash merge feature to main on psychedcms-core"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-core checkout main
      - git -C {{.PACKAGES_DIR}}/psychedcms-core pull origin main
      - git -C {{.PACKAGES_DIR}}/psychedcms-core merge --squash feature/{{.FEATURE}}
      - 'git -C {{.PACKAGES_DIR}}/psychedcms-core commit -m "feat: {{.FEATURE}}"'

  feature:push-merge:core:
    desc: Push merged main on psychedcms-core
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-core push origin main

  feature:cleanup:core:
    desc: "Delete feature branch on psychedcms-core (local and remote)"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-core branch -d feature/{{.FEATURE}} || true
      - git -C {{.PACKAGES_DIR}}/psychedcms-core push origin --delete feature/{{.FEATURE}} || true

  # ===========================================================================
  # Package Testing - psychedcms-workflow
  # ===========================================================================
  test:workflow:
    desc: Run tests for psychedcms-workflow package
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} exec php bin/phpunit {{.PACKAGES_DIR}}/psychedcms-workflow/tests/

  # ===========================================================================
  # Git - psychedcms-workflow (atomic operations)
  # ===========================================================================
  git:status:workflow:
    desc: Show git status for psychedcms-workflow
    cmds:
      - echo "=== psychedcms-workflow ===" && git -C {{.PACKAGES_DIR}}/psychedcms-workflow status -s && git -C {{.PACKAGES_DIR}}/psychedcms-workflow log -1 --oneline

  git:branch:workflow:
    desc: Show current branch for psychedcms-workflow
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-workflow branch --show-current

  # Feature branch operations
  feature:ensure:workflow:
    desc: "Ensure feature branch exists on psychedcms-workflow, create if needed"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - ../scripts/git-ensure-branch.sh {{.PACKAGES_DIR}}/psychedcms-workflow feature/{{.FEATURE}}

  feature:checkout:workflow:
    desc: "Checkout feature branch on psychedcms-workflow"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-workflow checkout feature/{{.FEATURE}}

  # Commit operations (with lazy branch creation)
  commit:workflow:
    desc: "Commit psychedcms-workflow with feature reference (creates branch if needed)"
    vars:
      FEATURE: '{{.FEATURE}}'
      MESSAGE: '{{.MESSAGE}}'
    cmds:
      - task: feature:ensure:workflow
        vars:
          FEATURE: '{{.FEATURE}}'
      - cmd: 'git -C {{.PACKAGES_DIR}}/psychedcms-workflow add .'
      - cmd: 'git -C {{.PACKAGES_DIR}}/psychedcms-workflow commit -m "[feature/{{.FEATURE}}] workflow: {{.MESSAGE}}"'

  # Push/Pull operations
  push:workflow:
    desc: Push psychedcms-workflow current branch
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-workflow push -u origin $(git -C {{.PACKAGES_DIR}}/psychedcms-workflow branch --show-current)

  pull:workflow:
    desc: 'Pull psychedcms-workflow (current branch or specified via BRANCH=x)'
    vars:
      BRANCH:
        sh: echo "${BRANCH:-$(git -C {{.PACKAGES_DIR}}/psychedcms-workflow branch --show-current)}"
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-workflow checkout {{.BRANCH}}
      - git -C {{.PACKAGES_DIR}}/psychedcms-workflow pull origin {{.BRANCH}}

  rebase:workflow:
    desc: "Rebase current feature branch onto main on psychedcms-workflow (updates main, rebases, force pushes)"
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-workflow fetch -p origin main:main
      - git -C {{.PACKAGES_DIR}}/psychedcms-workflow rebase main
      - git -C {{.PACKAGES_DIR}}/psychedcms-workflow push --force-with-lease origin $(git -C {{.PACKAGES_DIR}}/psychedcms-workflow branch --show-current)

  # Merge operations
  feature:merge:workflow:
    desc: "Squash merge feature to main on psychedcms-workflow"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-workflow checkout main
      - git -C {{.PACKAGES_DIR}}/psychedcms-workflow pull origin main
      - git -C {{.PACKAGES_DIR}}/psychedcms-workflow merge --squash feature/{{.FEATURE}}
      - 'git -C {{.PACKAGES_DIR}}/psychedcms-workflow commit -m "feat: {{.FEATURE}}"'

  feature:push-merge:workflow:
    desc: Push merged main on psychedcms-workflow
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-workflow push origin main

  feature:cleanup:workflow:
    desc: "Delete feature branch on psychedcms-workflow (local and remote)"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-workflow branch -d feature/{{.FEATURE}} || true
      - git -C {{.PACKAGES_DIR}}/psychedcms-workflow push origin --delete feature/{{.FEATURE}} || true

  # ===========================================================================
  # Package Testing - psychedcms-calendar
  # ===========================================================================
  test:calendar:
    desc: Run tests for psychedcms-calendar package
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} exec php bin/phpunit {{.PACKAGES_DIR}}/psychedcms-calendar/tests/

  # ===========================================================================
  # Git - psychedcms-calendar (atomic operations)
  # ===========================================================================
  git:status:calendar:
    desc: Show git status for psychedcms-calendar
    cmds:
      - echo "=== psychedcms-calendar ===" && git -C {{.PACKAGES_DIR}}/psychedcms-calendar status -s && git -C {{.PACKAGES_DIR}}/psychedcms-calendar log -1 --oneline

  git:branch:calendar:
    desc: Show current branch for psychedcms-calendar
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-calendar branch --show-current

  # Feature branch operations
  feature:ensure:calendar:
    desc: "Ensure feature branch exists on psychedcms-calendar, create if needed"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - ../scripts/git-ensure-branch.sh {{.PACKAGES_DIR}}/psychedcms-calendar feature/{{.FEATURE}}

  feature:checkout:calendar:
    desc: "Checkout feature branch on psychedcms-calendar"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-calendar checkout feature/{{.FEATURE}}

  # Commit operations (with lazy branch creation)
  commit:calendar:
    desc: "Commit psychedcms-calendar with feature reference (creates branch if needed)"
    vars:
      FEATURE: '{{.FEATURE}}'
      MESSAGE: '{{.MESSAGE}}'
    cmds:
      - task: feature:ensure:calendar
        vars:
          FEATURE: '{{.FEATURE}}'
      - cmd: 'git -C {{.PACKAGES_DIR}}/psychedcms-calendar add .'
      - cmd: 'git -C {{.PACKAGES_DIR}}/psychedcms-calendar commit -m "[feature/{{.FEATURE}}] calendar: {{.MESSAGE}}"'

  # Push/Pull operations
  push:calendar:
    desc: Push psychedcms-calendar current branch
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-calendar push -u origin $(git -C {{.PACKAGES_DIR}}/psychedcms-calendar branch --show-current)

  pull:calendar:
    desc: 'Pull psychedcms-calendar (current branch or specified via BRANCH=x)'
    vars:
      BRANCH:
        sh: echo "${BRANCH:-$(git -C {{.PACKAGES_DIR}}/psychedcms-calendar branch --show-current)}"
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-calendar checkout {{.BRANCH}}
      - git -C {{.PACKAGES_DIR}}/psychedcms-calendar pull origin {{.BRANCH}}

  rebase:calendar:
    desc: "Rebase current feature branch onto main on psychedcms-calendar (updates main, rebases, force pushes)"
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-calendar fetch -p origin main:main
      - git -C {{.PACKAGES_DIR}}/psychedcms-calendar rebase main
      - git -C {{.PACKAGES_DIR}}/psychedcms-calendar push --force-with-lease origin $(git -C {{.PACKAGES_DIR}}/psychedcms-calendar branch --show-current)

  # Merge operations
  feature:merge:calendar:
    desc: "Squash merge feature to main on psychedcms-calendar"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-calendar checkout main
      - git -C {{.PACKAGES_DIR}}/psychedcms-calendar pull origin main
      - git -C {{.PACKAGES_DIR}}/psychedcms-calendar merge --squash feature/{{.FEATURE}}
      - 'git -C {{.PACKAGES_DIR}}/psychedcms-calendar commit -m "feat: {{.FEATURE}}"'

  feature:push-merge:calendar:
    desc: Push merged main on psychedcms-calendar
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-calendar push origin main

  feature:cleanup:calendar:
    desc: "Delete feature branch on psychedcms-calendar (local and remote)"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-calendar branch -d feature/{{.FEATURE}} || true
      - git -C {{.PACKAGES_DIR}}/psychedcms-calendar push origin --delete feature/{{.FEATURE}} || true

  # ===========================================================================
  # Git - psychedcms-admin-core (atomic operations)
  # ===========================================================================
  git:status:admin-core:
    desc: Show git status for psychedcms-admin-core
    cmds:
      - echo "=== psychedcms-admin-core ===" && git -C {{.PACKAGES_DIR}}/psychedcms-admin-core status -s && git -C {{.PACKAGES_DIR}}/psychedcms-admin-core log -1 --oneline

  git:branch:admin-core:
    desc: Show current branch for psychedcms-admin-core
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-admin-core branch --show-current

  commit:admin-core:
    desc: "Commit psychedcms-admin-core on current branch"
    vars:
      MESSAGE: '{{.MESSAGE}}'
    cmds:
      - cmd: 'git -C {{.PACKAGES_DIR}}/psychedcms-admin-core add .'
      - cmd: 'git -C {{.PACKAGES_DIR}}/psychedcms-admin-core commit -m "{{.MESSAGE}}"'

  push:admin-core:
    desc: Push psychedcms-admin-core current branch
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-admin-core push -u origin $(git -C {{.PACKAGES_DIR}}/psychedcms-admin-core branch --show-current)

  pull:admin-core:
    desc: 'Pull psychedcms-admin-core (current branch or specified via BRANCH=x)'
    vars:
      BRANCH:
        sh: echo "${BRANCH:-$(git -C {{.PACKAGES_DIR}}/psychedcms-admin-core branch --show-current)}"
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-admin-core checkout {{.BRANCH}}
      - git -C {{.PACKAGES_DIR}}/psychedcms-admin-core pull origin {{.BRANCH}}

  # ===========================================================================
  # Git - psychedcms-admin-translatable (atomic operations)
  # ===========================================================================
  git:status:admin-translatable:
    desc: Show git status for psychedcms-admin-translatable
    cmds:
      - echo "=== psychedcms-admin-translatable ===" && git -C {{.PACKAGES_DIR}}/psychedcms-admin-translatable status -s && git -C {{.PACKAGES_DIR}}/psychedcms-admin-translatable log -1 --oneline

  git:branch:admin-translatable:
    desc: Show current branch for psychedcms-admin-translatable
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-admin-translatable branch --show-current

  commit:admin-translatable:
    desc: "Commit psychedcms-admin-translatable on current branch"
    vars:
      MESSAGE: '{{.MESSAGE}}'
    cmds:
      - cmd: 'git -C {{.PACKAGES_DIR}}/psychedcms-admin-translatable add .'
      - cmd: 'git -C {{.PACKAGES_DIR}}/psychedcms-admin-translatable commit -m "{{.MESSAGE}}"'

  push:admin-translatable:
    desc: Push psychedcms-admin-translatable current branch
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-admin-translatable push -u origin $(git -C {{.PACKAGES_DIR}}/psychedcms-admin-translatable branch --show-current)

  pull:admin-translatable:
    desc: 'Pull psychedcms-admin-translatable (current branch or specified via BRANCH=x)'
    vars:
      BRANCH:
        sh: echo "${BRANCH:-$(git -C {{.PACKAGES_DIR}}/psychedcms-admin-translatable branch --show-current)}"
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-admin-translatable checkout {{.BRANCH}}
      - git -C {{.PACKAGES_DIR}}/psychedcms-admin-translatable pull origin {{.BRANCH}}

  # ===========================================================================
  # Git - psychedcms-admin-geolocation (atomic operations)
  # ===========================================================================
  git:status:admin-geolocation:
    desc: Show git status for psychedcms-admin-geolocation
    cmds:
      - echo "=== psychedcms-admin-geolocation ===" && git -C {{.PACKAGES_DIR}}/psychedcms-admin-geolocation status -s && git -C {{.PACKAGES_DIR}}/psychedcms-admin-geolocation log -1 --oneline

  git:branch:admin-geolocation:
    desc: Show current branch for psychedcms-admin-geolocation
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-admin-geolocation branch --show-current

  commit:admin-geolocation:
    desc: "Commit psychedcms-admin-geolocation on current branch"
    vars:
      MESSAGE: '{{.MESSAGE}}'
    cmds:
      - cmd: 'git -C {{.PACKAGES_DIR}}/psychedcms-admin-geolocation add .'
      - cmd: 'git -C {{.PACKAGES_DIR}}/psychedcms-admin-geolocation commit -m "{{.MESSAGE}}"'

  push:admin-geolocation:
    desc: Push psychedcms-admin-geolocation current branch
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-admin-geolocation push -u origin $(git -C {{.PACKAGES_DIR}}/psychedcms-admin-geolocation branch --show-current)

  pull:admin-geolocation:
    desc: 'Pull psychedcms-admin-geolocation (current branch or specified via BRANCH=x)'
    vars:
      BRANCH:
        sh: echo "${BRANCH:-$(git -C {{.PACKAGES_DIR}}/psychedcms-admin-geolocation branch --show-current)}"
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-admin-geolocation checkout {{.BRANCH}}
      - git -C {{.PACKAGES_DIR}}/psychedcms-admin-geolocation pull origin {{.BRANCH}}

  # ===========================================================================
  # Git - psychedcms-geolocation (atomic operations)
  # ===========================================================================
  git:status:geolocation:
    desc: Show git status for psychedcms-geolocation
    cmds:
      - echo "=== psychedcms-geolocation ===" && git -C {{.PACKAGES_DIR}}/psychedcms-geolocation status -s && git -C {{.PACKAGES_DIR}}/psychedcms-geolocation log -1 --oneline

  git:branch:geolocation:
    desc: Show current branch for psychedcms-geolocation
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-geolocation branch --show-current

  feature:ensure:geolocation:
    desc: "Ensure feature branch exists on psychedcms-geolocation, create if needed"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - ../scripts/git-ensure-branch.sh {{.PACKAGES_DIR}}/psychedcms-geolocation feature/{{.FEATURE}}

  feature:checkout:geolocation:
    desc: "Checkout feature branch on psychedcms-geolocation"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-geolocation checkout feature/{{.FEATURE}}

  commit:geolocation:
    desc: "Commit psychedcms-geolocation with feature reference (creates branch if needed)"
    vars:
      FEATURE: '{{.FEATURE}}'
      MESSAGE: '{{.MESSAGE}}'
    cmds:
      - task: feature:ensure:geolocation
        vars:
          FEATURE: '{{.FEATURE}}'
      - cmd: 'git -C {{.PACKAGES_DIR}}/psychedcms-geolocation add .'
      - cmd: 'git -C {{.PACKAGES_DIR}}/psychedcms-geolocation commit -m "[feature/{{.FEATURE}}] geolocation: {{.MESSAGE}}"'

  push:geolocation:
    desc: Push psychedcms-geolocation current branch
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-geolocation push -u origin $(git -C {{.PACKAGES_DIR}}/psychedcms-geolocation branch --show-current)

  pull:geolocation:
    desc: 'Pull psychedcms-geolocation (current branch or specified via BRANCH=x)'
    vars:
      BRANCH:
        sh: echo "${BRANCH:-$(git -C {{.PACKAGES_DIR}}/psychedcms-geolocation branch --show-current)}"
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-geolocation checkout {{.BRANCH}}
      - git -C {{.PACKAGES_DIR}}/psychedcms-geolocation pull origin {{.BRANCH}}

  rebase:geolocation:
    desc: "Rebase current feature branch onto main on psychedcms-geolocation (updates main, rebases, force pushes)"
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-geolocation fetch -p origin main:main
      - git -C {{.PACKAGES_DIR}}/psychedcms-geolocation rebase main
      - git -C {{.PACKAGES_DIR}}/psychedcms-geolocation push --force-with-lease origin $(git -C {{.PACKAGES_DIR}}/psychedcms-geolocation branch --show-current)

  feature:merge:geolocation:
    desc: "Squash merge feature to main on psychedcms-geolocation"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-geolocation checkout main
      - git -C {{.PACKAGES_DIR}}/psychedcms-geolocation pull origin main
      - git -C {{.PACKAGES_DIR}}/psychedcms-geolocation merge --squash feature/{{.FEATURE}}
      - 'git -C {{.PACKAGES_DIR}}/psychedcms-geolocation commit -m "feat: {{.FEATURE}}"'

  feature:push-merge:geolocation:
    desc: Push merged main on psychedcms-geolocation
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-geolocation push origin main

  feature:cleanup:geolocation:
    desc: "Delete feature branch on psychedcms-geolocation (local and remote)"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-geolocation branch -d feature/{{.FEATURE}} || true
      - git -C {{.PACKAGES_DIR}}/psychedcms-geolocation push origin --delete feature/{{.FEATURE}} || true

  # ===========================================================================
  # Git - psychedcms-media (atomic operations)
  # ===========================================================================
  git:status:media:
    desc: Show git status for psychedcms-media
    cmds:
      - echo "=== psychedcms-media ===" && git -C {{.PACKAGES_DIR}}/psychedcms-media status -s && git -C {{.PACKAGES_DIR}}/psychedcms-media log -1 --oneline

  git:branch:media:
    desc: Show current branch for psychedcms-media
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-media branch --show-current

  feature:ensure:media:
    desc: "Ensure feature branch exists on psychedcms-media, create if needed"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - ../scripts/git-ensure-branch.sh {{.PACKAGES_DIR}}/psychedcms-media feature/{{.FEATURE}}

  feature:checkout:media:
    desc: "Checkout feature branch on psychedcms-media"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-media checkout feature/{{.FEATURE}}

  commit:media:
    desc: "Commit psychedcms-media with feature reference (creates branch if needed)"
    vars:
      FEATURE: '{{.FEATURE}}'
      MESSAGE: '{{.MESSAGE}}'
    cmds:
      - task: feature:ensure:media
        vars:
          FEATURE: '{{.FEATURE}}'
      - cmd: 'git -C {{.PACKAGES_DIR}}/psychedcms-media add .'
      - cmd: 'git -C {{.PACKAGES_DIR}}/psychedcms-media commit -m "[feature/{{.FEATURE}}] media: {{.MESSAGE}}"'

  push:media:
    desc: Push psychedcms-media current branch
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-media push -u origin $(git -C {{.PACKAGES_DIR}}/psychedcms-media branch --show-current)

  pull:media:
    desc: 'Pull psychedcms-media (current branch or specified via BRANCH=x)'
    vars:
      BRANCH:
        sh: echo "${BRANCH:-$(git -C {{.PACKAGES_DIR}}/psychedcms-media branch --show-current)}"
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-media checkout {{.BRANCH}}
      - git -C {{.PACKAGES_DIR}}/psychedcms-media pull origin {{.BRANCH}}

  rebase:media:
    desc: "Rebase current feature branch onto main on psychedcms-media (updates main, rebases, force pushes)"
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-media fetch -p origin main:main
      - git -C {{.PACKAGES_DIR}}/psychedcms-media rebase main
      - git -C {{.PACKAGES_DIR}}/psychedcms-media push --force-with-lease origin $(git -C {{.PACKAGES_DIR}}/psychedcms-media branch --show-current)

  feature:merge:media:
    desc: "Squash merge feature to main on psychedcms-media"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-media checkout main
      - git -C {{.PACKAGES_DIR}}/psychedcms-media pull origin main
      - git -C {{.PACKAGES_DIR}}/psychedcms-media merge --squash feature/{{.FEATURE}}
      - 'git -C {{.PACKAGES_DIR}}/psychedcms-media commit -m "feat: {{.FEATURE}}"'

  feature:push-merge:media:
    desc: Push merged main on psychedcms-media
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-media push origin main

  feature:cleanup:media:
    desc: "Delete feature branch on psychedcms-media (local and remote)"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-media branch -d feature/{{.FEATURE}} || true
      - git -C {{.PACKAGES_DIR}}/psychedcms-media push origin --delete feature/{{.FEATURE}} || true

  # ===========================================================================
  # Git - psychedcms-taxonomy (atomic operations)
  # ===========================================================================
  git:status:taxonomy:
    desc: Show git status for psychedcms-taxonomy
    cmds:
      - echo "=== psychedcms-taxonomy ===" && git -C {{.PACKAGES_DIR}}/psychedcms-taxonomy status -s && git -C {{.PACKAGES_DIR}}/psychedcms-taxonomy log -1 --oneline

  git:branch:taxonomy:
    desc: Show current branch for psychedcms-taxonomy
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-taxonomy branch --show-current

  feature:ensure:taxonomy:
    desc: "Ensure feature branch exists on psychedcms-taxonomy, create if needed"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - ../scripts/git-ensure-branch.sh {{.PACKAGES_DIR}}/psychedcms-taxonomy feature/{{.FEATURE}}

  feature:checkout:taxonomy:
    desc: "Checkout feature branch on psychedcms-taxonomy"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-taxonomy checkout feature/{{.FEATURE}}

  commit:taxonomy:
    desc: "Commit psychedcms-taxonomy with feature reference (creates branch if needed)"
    vars:
      FEATURE: '{{.FEATURE}}'
      MESSAGE: '{{.MESSAGE}}'
    cmds:
      - task: feature:ensure:taxonomy
        vars:
          FEATURE: '{{.FEATURE}}'
      - cmd: 'git -C {{.PACKAGES_DIR}}/psychedcms-taxonomy add .'
      - cmd: 'git -C {{.PACKAGES_DIR}}/psychedcms-taxonomy commit -m "[feature/{{.FEATURE}}] taxonomy: {{.MESSAGE}}"'

  push:taxonomy:
    desc: Push psychedcms-taxonomy current branch
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-taxonomy push -u origin $(git -C {{.PACKAGES_DIR}}/psychedcms-taxonomy branch --show-current)

  pull:taxonomy:
    desc: 'Pull psychedcms-taxonomy (current branch or specified via BRANCH=x)'
    vars:
      BRANCH:
        sh: echo "${BRANCH:-$(git -C {{.PACKAGES_DIR}}/psychedcms-taxonomy branch --show-current)}"
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-taxonomy checkout {{.BRANCH}}
      - git -C {{.PACKAGES_DIR}}/psychedcms-taxonomy pull origin {{.BRANCH}}

  rebase:taxonomy:
    desc: "Rebase current feature branch onto main on psychedcms-taxonomy (updates main, rebases, force pushes)"
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-taxonomy fetch -p origin main:main
      - git -C {{.PACKAGES_DIR}}/psychedcms-taxonomy rebase main
      - git -C {{.PACKAGES_DIR}}/psychedcms-taxonomy push --force-with-lease origin $(git -C {{.PACKAGES_DIR}}/psychedcms-taxonomy branch --show-current)

  feature:merge:taxonomy:
    desc: "Squash merge feature to main on psychedcms-taxonomy"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-taxonomy checkout main
      - git -C {{.PACKAGES_DIR}}/psychedcms-taxonomy pull origin main
      - git -C {{.PACKAGES_DIR}}/psychedcms-taxonomy merge --squash feature/{{.FEATURE}}
      - 'git -C {{.PACKAGES_DIR}}/psychedcms-taxonomy commit -m "feat: {{.FEATURE}}"'

  feature:push-merge:taxonomy:
    desc: Push merged main on psychedcms-taxonomy
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-taxonomy push origin main

  feature:cleanup:taxonomy:
    desc: "Delete feature branch on psychedcms-taxonomy (local and remote)"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-taxonomy branch -d feature/{{.FEATURE}} || true
      - git -C {{.PACKAGES_DIR}}/psychedcms-taxonomy push origin --delete feature/{{.FEATURE}} || true

  # ===========================================================================
  # Git - psychedcms-translatable (atomic operations)
  # ===========================================================================
  git:status:translatable:
    desc: Show git status for psychedcms-translatable
    cmds:
      - echo "=== psychedcms-translatable ===" && git -C {{.PACKAGES_DIR}}/psychedcms-translatable status -s && git -C {{.PACKAGES_DIR}}/psychedcms-translatable log -1 --oneline

  git:branch:translatable:
    desc: Show current branch for psychedcms-translatable
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-translatable branch --show-current

  feature:ensure:translatable:
    desc: "Ensure feature branch exists on psychedcms-translatable, create if needed"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - ../scripts/git-ensure-branch.sh {{.PACKAGES_DIR}}/psychedcms-translatable feature/{{.FEATURE}}

  feature:checkout:translatable:
    desc: "Checkout feature branch on psychedcms-translatable"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-translatable checkout feature/{{.FEATURE}}

  commit:translatable:
    desc: "Commit psychedcms-translatable with feature reference (creates branch if needed)"
    vars:
      FEATURE: '{{.FEATURE}}'
      MESSAGE: '{{.MESSAGE}}'
    cmds:
      - task: feature:ensure:translatable
        vars:
          FEATURE: '{{.FEATURE}}'
      - cmd: 'git -C {{.PACKAGES_DIR}}/psychedcms-translatable add .'
      - cmd: 'git -C {{.PACKAGES_DIR}}/psychedcms-translatable commit -m "[feature/{{.FEATURE}}] translatable: {{.MESSAGE}}"'

  push:translatable:
    desc: Push psychedcms-translatable current branch
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-translatable push -u origin $(git -C {{.PACKAGES_DIR}}/psychedcms-translatable branch --show-current)

  pull:translatable:
    desc: 'Pull psychedcms-translatable (current branch or specified via BRANCH=x)'
    vars:
      BRANCH:
        sh: echo "${BRANCH:-$(git -C {{.PACKAGES_DIR}}/psychedcms-translatable branch --show-current)}"
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-translatable checkout {{.BRANCH}}
      - git -C {{.PACKAGES_DIR}}/psychedcms-translatable pull origin {{.BRANCH}}

  rebase:translatable:
    desc: "Rebase current feature branch onto main on psychedcms-translatable (updates main, rebases, force pushes)"
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-translatable fetch -p origin main:main
      - git -C {{.PACKAGES_DIR}}/psychedcms-translatable rebase main
      - git -C {{.PACKAGES_DIR}}/psychedcms-translatable push --force-with-lease origin $(git -C {{.PACKAGES_DIR}}/psychedcms-translatable branch --show-current)

  feature:merge:translatable:
    desc: "Squash merge feature to main on psychedcms-translatable"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-translatable checkout main
      - git -C {{.PACKAGES_DIR}}/psychedcms-translatable pull origin main
      - git -C {{.PACKAGES_DIR}}/psychedcms-translatable merge --squash feature/{{.FEATURE}}
      - 'git -C {{.PACKAGES_DIR}}/psychedcms-translatable commit -m "feat: {{.FEATURE}}"'

  feature:push-merge:translatable:
    desc: Push merged main on psychedcms-translatable
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-translatable push origin main

  feature:cleanup:translatable:
    desc: "Delete feature branch on psychedcms-translatable (local and remote)"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-translatable branch -d feature/{{.FEATURE}} || true
      - git -C {{.PACKAGES_DIR}}/psychedcms-translatable push origin --delete feature/{{.FEATURE}} || true

  # ===========================================================================
  # Package Testing - psychedcms-search
  # ===========================================================================
  test:search:
    desc: Run tests for psychedcms-search package
    cmds:
      - docker compose -f {{.COMPOSE_FILE}} exec php bin/phpunit {{.PACKAGES_DIR}}/psychedcms-search/tests/

  # ===========================================================================
  # Git - psychedcms-search (atomic operations)
  # ===========================================================================
  git:status:search:
    desc: Show git status for psychedcms-search
    cmds:
      - echo "=== psychedcms-search ===" && git -C {{.PACKAGES_DIR}}/psychedcms-search status -s && git -C {{.PACKAGES_DIR}}/psychedcms-search log -1 --oneline

  git:branch:search:
    desc: Show current branch for psychedcms-search
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-search branch --show-current

  feature:ensure:search:
    desc: "Ensure feature branch exists on psychedcms-search, create if needed"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - ../scripts/git-ensure-branch.sh {{.PACKAGES_DIR}}/psychedcms-search feature/{{.FEATURE}}

  feature:checkout:search:
    desc: "Checkout feature branch on psychedcms-search"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-search checkout feature/{{.FEATURE}}

  commit:search:
    desc: "Commit psychedcms-search with feature reference (creates branch if needed)"
    vars:
      FEATURE: '{{.FEATURE}}'
      MESSAGE: '{{.MESSAGE}}'
    cmds:
      - task: feature:ensure:search
        vars:
          FEATURE: '{{.FEATURE}}'
      - cmd: 'git -C {{.PACKAGES_DIR}}/psychedcms-search add .'
      - cmd: 'git -C {{.PACKAGES_DIR}}/psychedcms-search commit -m "[feature/{{.FEATURE}}] search: {{.MESSAGE}}"'

  push:search:
    desc: Push psychedcms-search current branch
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-search push -u origin $(git -C {{.PACKAGES_DIR}}/psychedcms-search branch --show-current)

  pull:search:
    desc: 'Pull psychedcms-search (current branch or specified via BRANCH=x)'
    vars:
      BRANCH:
        sh: echo "${BRANCH:-$(git -C {{.PACKAGES_DIR}}/psychedcms-search branch --show-current)}"
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-search checkout {{.BRANCH}}
      - git -C {{.PACKAGES_DIR}}/psychedcms-search pull origin {{.BRANCH}}

  rebase:search:
    desc: "Rebase current feature branch onto main on psychedcms-search (updates main, rebases, force pushes)"
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-search fetch -p origin main:main
      - git -C {{.PACKAGES_DIR}}/psychedcms-search rebase main
      - git -C {{.PACKAGES_DIR}}/psychedcms-search push --force-with-lease origin $(git -C {{.PACKAGES_DIR}}/psychedcms-search branch --show-current)

  feature:merge:search:
    desc: "Squash merge feature to main on psychedcms-search"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-search checkout main
      - git -C {{.PACKAGES_DIR}}/psychedcms-search pull origin main
      - git -C {{.PACKAGES_DIR}}/psychedcms-search merge --squash feature/{{.FEATURE}}
      - 'git -C {{.PACKAGES_DIR}}/psychedcms-search commit -m "feat: {{.FEATURE}}"'

  feature:push-merge:search:
    desc: Push merged main on psychedcms-search
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-search push origin main

  feature:cleanup:search:
    desc: "Delete feature branch on psychedcms-search (local and remote)"
    vars:
      FEATURE: '{{.FEATURE}}'
    cmds:
      - git -C {{.PACKAGES_DIR}}/psychedcms-search branch -d feature/{{.FEATURE}} || true
      - git -C {{.PACKAGES_DIR}}/psychedcms-search push origin --delete feature/{{.FEATURE}} || true
